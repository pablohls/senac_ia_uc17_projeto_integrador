# Story 5.6: Harness de benchmark A/B/C dos modelos [COULD]

## Status

Done

## Executor Assignment

```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [git, ruff, pytest]
```

> **Nota da equipe:** Dificuldade 🟡 (**intermediária** — script de avaliação). Prioridade **COULD** (opcional/**não-bloqueante**: o ADR-002 já firmou `qwen2.5:14b`; este teste **valida** a escolha e vira evidência para a banca). **Depende da Story 5.1 (`chat()`)** e, para conteúdo real, da 5.2/5.4. Referência: `docs/research/2026-07-08-benchmark-llm-local/protocolo-empirico.md`, `findings.md`.

## Story

**As a** equipe do projeto,
**I want** medir Qwen2.5-14B × Qwen3.5-9B × Gemma 3-12B **no nosso próprio corpus e tarefas**,
**so that** a escolha do modelo seja justificada por **dado com avaliação cega** — e não pelo "mais novo".

## Acceptance Criteria

1. `scripts/benchmark_llm.py` executa o protocolo (`protocolo-empirico.md`): para cada modelo da shortlist × amostra congelada, gera as saídas das tarefas T1 (rótulo), T2 ("por que sobe") e T3 (RAG), **reaproveitando `src/common/llm.py`** (só troca `base_url`/`model`).
2. **Amostra congelada** em `amostra_benchmark.json` (≈12 tópicos estratificados + 8 perguntas RAG, sendo 3 "pegadinhas"), fixa para os 3 modelos.
3. Coleta automática de **latência, throughput e VRAM de pico** por tarefa/modelo em `resultados_brutos.parquet`.
4. Gera **planilha de avaliação cega** (saídas embaralhadas/anonimizadas A/B/C) para pontuação humana pela rubrica 1–5.
5. Produz `resultado-benchmark.md` (no diretório de pesquisa) com a tabela consolidada e a recomendação; se o vencedor **não** for `qwen2.5:14b`, registrar a troca sugerida de `insight.model` e anotar no ADR-002 (Change Log).
6. **Declara limitações** (amostra pequena → sinal qualitativo, não significância estatística). Script reproduzível; não quebra se um modelo estiver ausente (pula com log).

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled — validação por revisão manual (padrão do projeto).

## Tasks / Subtasks

- [x] **Task 1 — Amostra congelada** (AC: 2)
  - [x] Selecionar ≈12 tópicos estratificados (topo, anomalia T35, ruidosos T5/T12, nicho, multi-tema) + 8 perguntas RAG (5 respondíveis + 3 pegadinhas) → `amostra_benchmark.json`.
- [x] **Task 2 — Execução multi-modelo** (AC: 1, 3, 6)
  - [x] Loop {3 modelos × amostra × T1/T2/T3} via `llm.py`; medir latência/throughput/VRAM (`nvidia-smi`); gravar `resultados_brutos.parquet`; pular modelo ausente com log.
- [x] **Task 3 — Planilha cega** (AC: 4)
  - [x] Embaralhar/anonimizar saídas (A/B/C ocultos) para avaliação humana pela rubrica.
- [x] **Task 4 — Consolidação** (AC: 5, 6)
  - [x] `resultado-benchmark.md`: médias por critério + latência/VRAM + recomendação + limitações.

## 📘 Guia de Desenvolvimento (para o integrante)

> Dificuldade 🟡 — **intermediária**, e **opcional**. Se o prazo apertar, pule: o ADR já tem um default defensável. Se der tempo, esta story rende uma **narrativa de rigor** forte na banca.

### 🎯 O que esta story faz
Roda os 3 modelos candidatos sobre os **mesmos** tópicos e perguntas, guarda as respostas e mede tempo/VRAM. Depois, duas pessoas da equipe pontuam as respostas **sem saber qual modelo é qual** (avaliação cega). No fim, um relatório diz qual venceu e por quê.

### 🧠 Pontos de rigor (não pular)
- **Avaliação cega (AC4):** embaralhe as saídas antes de pontuar — senão o viés ("gosto do Qwen") contamina o resultado.
- **Mesma amostra, mesmo prompt (AC1, AC2):** só o **modelo** muda. Congele a amostra num arquivo.
- **Fidelidade é eliminatória:** modelo que alucina no RAG (pegadinhas) perde, mesmo se escreve bonito.
- **Reúso (AC1):** use o `llm.py` da 5.1 — não escreva um cliente novo.
- **Honestidade (AC6):** 12 tópicos dão *sinal*, não prova estatística. Diga isso no relatório.

### 🪜 Passo a passo (resumido — protocolo-empirico.md)
1. Congelar `amostra_benchmark.json`.
2. `ollama pull` dos 3 modelos.
3. Loop modelo×amostra×tarefa via `llm.py`; medir tempo/VRAM.
4. Gerar planilha cega; 2 avaliadores pontuam (rubrica 1–5).
5. Consolidar `resultado-benchmark.md` + recomendação.

### ✅ Como saber que funcionou
- `resultados_brutos.parquet` tem as saídas dos 3 modelos para toda a amostra. *(AC1, AC3)*
- A planilha cega não revela qual é qual. *(AC4)*
- `resultado-benchmark.md` conclui com um vencedor + limitações declaradas. *(AC5, AC6)*

### ⚠️ Erros comuns
- **Avaliar sem cegar:** invalida o resultado.
- **Comparar quantizações diferentes:** padronize Q4.
- **Tunar o prompt por modelo:** o prompt é o **mesmo** para todos.

### 📚 Glossário
| Termo | Significado simples |
|---|---|
| Avaliação cega | Pontuar sem saber qual modelo produziu a saída |
| Rubrica | Régua de notas (1–5) por critério |
| Throughput | Tokens por segundo |
| Eliminatório | Critério que reprova sozinho (aqui: alucinar) |

## Dev Notes

> Contexto de `docs/research/2026-07-08-benchmark-llm-local/protocolo-empirico.md` e `findings.md`. NÃO inventar — o protocolo é a fonte.

- **Owner:** `scripts/benchmark_llm.py` (novo) + artefatos em `docs/research/2026-07-08-benchmark-llm-local/`. [Source: protocolo-empirico.md §6, §8]
- **Shortlist:** `qwen2.5:14b` (default), `qwen3.5:9b`, `gemma3:12b`. [Source: adr-002 §Decisão; findings.md §5]
- **Reúso:** `src/common/llm.py` (5.1) para todas as chamadas; amostra a partir de `scores.parquet`/`topic_info`/`doc_topics`. [Source: protocolo-empirico.md §3]
- **Regra de decisão:** qualidade (peso em fidelidade+fluência) → empate resolve por latência/VRAM → fidelidade eliminatória. [Source: protocolo-empirico.md §7]
- **Não-bloqueante:** resultado só ajusta `config.insight.model` se divergir do default. [Source: adr-002 §Decisão]

### Testing
- **Local:** o próprio script é a ferramenta de avaliação; teste leve da montagem da amostra e do embaralhamento cego (determinístico com seed passada por parâmetro). Não há unit-test de "qualidade" (isso é a avaliação humana). [Source: protocolo-empirico.md §9]

### Project Structure Notes
- `scripts/` já existe no projeto (utilitários fora do pacote `src`). Artefatos de resultado moram no diretório de pesquisa, não em `dados/`. Sem conflito. [Source: project-analysis.md §1]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-08 | 0.1 | Draft inicial — harness de benchmark A/B/C (opcional, não-bloqueante) | @sm |
| 2026-07-08 | 0.2 | Validated GO (8/10) — Status: Draft → Ready. COULD/não-bloqueante; qualidade depende de avaliação humana (fora de teste automatizado), o que é adequado | @po |
| 2026-07-09 | 0.3 | Início da implementação — Status: Ready → InProgress | @dev |
| 2026-07-09 | 1.0 | Implementação completa (4 tasks, 6 ACs): harness + amostra congelada + 96 medições reais dos 3 modelos + planilha cega + relatório. 153 testes verdes (142 + 11 novos), ruff limpo. Achado: qwen3.5:9b (thinking) incompatível com contrato de produção. Recomendação final aguarda avaliação cega humana (≥2 avaliadores). Status: InProgress → InReview | @dev |
| 2026-07-09 | 1.1 | Avaliação cega humana formalmente **dispensada por decisão da equipe** (waiver): qwen3.5:9b eliminado por confiabilidade (34% falha; 0% saída sob contrato de produção) e vencedor em desempenho = default do ADR-002 — a avaliação cega só justificaria uma *troca* de modelo. Relatório fechado com recomendação final: **manter `qwen2.5:14b`**. Planilha cega preservada no repo para eventual execução a posteriori. | @tech-lead |
| 2026-07-09 | 1.2 | Quality gate **WAIVED** (6/6 ACs entregues; 153 testes verdes; ruff limpo). Waiver: avaliação de qualidade textual humana dispensada e documentada (Change Log 1.1 + relatório §Qualidade). Sem issues bloqueantes. Status: InReview → Done | @architect |

## Registro de Implementação
### Ambiente e Proveniência
@dev, sessão VM GPU 16GB
### Debug Log References
- 1ª execução real: `qwen3.5:9b` falhou 100% — diagnóstico: modelo *thinking* devolve raciocínio em `delta.reasoning` e estourava o `max_tokens=512` de produção ANTES do 1º token de conteúdo (`finish_reason=length`, 435 chunks de reasoning, 0 de conteúdo). Correção: budget uniforme `MAX_TOKENS_BENCHMARK=4096` para os 3 modelos (nada tunado por modelo).
- 2ª execução (definitiva): 96 medições, 3 modelos completos. `qwen3.5:9b` ainda falhou 11/32 chamadas (estourou 4096 só raciocinando, ~35s cada) — achado registrado na seção Confiabilidade do relatório.
### Completion Notes List
- **AC1:** harness com 4 subcomandos (`amostra`/`executar`/`planilha`/`consolidar`); TODAS as chamadas via `chat_stream()` do `llm.py` (5.1) — o único ponto que varia é `config.insight.model` (restaurado em `finally`). Contexto de tópicos reusa `montar_contexto`/`limpar_label` (5.2); prompt RAG reusa `montar_prompt_rag` + `retrieve` (5.4/5.5). T2 usa o rótulo que o PRÓPRIO modelo deu em T1 (espelha produção). Prompts RAG pré-computados uma única vez (retriever idêntico para os 3 modelos).
- **AC2:** amostra congelada determinística: 6 topo + anomalia T35 + ruidosos T5/T12 + nicho T146/T147 (menores sizes sem suporte) + multi-tema T0; 8 perguntas RAG (5 respondíveis + 3 pegadinhas reusadas do E2E/gate da 5.5).
- **AC3:** latência 1º token de conteúdo + total, throughput (~1 token/chunk, aproximação declarada) e VRAM (`nvidia-smi`, `memory.used`) por chamada; `ollama stop` entre modelos para medição justa; 96 linhas em `resultados_brutos.parquet`.
- **AC4:** `avaliacao_cega.csv` (85 linhas avaliáveis) com opções embaralhadas POR ITEM (seed 42, determinístico — testado), sem coluna de modelo; vínculo só em `mapa_cego.json` (não abrir antes de pontuar).
- **AC5:** `resultado-benchmark.md` gerado com desempenho + confiabilidade consolidados; regra de decisão do protocolo §7 implementada em `decidir()` (fidelidade < 3 eliminatória, empate ≤ 0,3 → latência) com alerta automático de troca de `insight.model`/ADR-002 se o vencedor divergir do default. **Recomendação final pendente da avaliação cega humana** (≥ 2 integrantes, ~1-2h — protocolo §6.4/§10): `consolidar --avaliacoes <csvs>` fecha o relatório.
- **AC6:** limitações declaradas no relatório (amostra pequena, Q4, aproximação de tokens, budget do harness); modelo ausente é pulado com log (validado: endpoint consultado via `models.list()`); `nvidia-smi` ausente ⇒ métrica vazia; curto-circuito à la Analista IA se o modelo presente não responde.
- **Achado empírico central (evidência para a banca):** sob o contrato de produção (512 tokens), o `qwen3.5:9b` NÃO produz saída alguma (thinking consome o budget); mesmo com 8× o budget, falha em 34% das chamadas e tem latência de ~19s até o 1º token útil (vs 0,6–2,2s dos demais). Desempenho: `qwen2.5:14b` lidera latência/throughput; `gemma3:12b` próximo; os 3 recusaram honestamente as 3 pegadinhas RAG.
- **Carry-forward da 5.5 (artefato "[1]" após recusa):** não tratado aqui — o prompt de produção é insumo congelado do benchmark (mudá-lo agora invalidaria a comparação); segue como ajuste fino pós-benchmark.
### File List
- `scripts/benchmark_llm.py` **[novo]** — harness (amostra, execução, planilha cega, consolidação)
- `tests/test_benchmark_llm.py` **[novo]** — 11 testes (amostra estratificada, embaralhamento cego determinístico, medição, regra de decisão)
- `docs/research/2026-07-08-benchmark-llm-local/amostra_benchmark.json` **[novo]** — amostra congelada (12 tópicos + 8 perguntas)
- `docs/research/2026-07-08-benchmark-llm-local/resultados_brutos.parquet` **[novo]** — 96 medições reais (3 modelos)
- `docs/research/2026-07-08-benchmark-llm-local/avaliacao_cega.csv` **[novo]** — planilha cega p/ rubrica 1–5
- `docs/research/2026-07-08-benchmark-llm-local/mapa_cego.json` **[novo]** — mapa opção→modelo (abrir só após pontuar)
- `docs/research/2026-07-08-benchmark-llm-local/resultado-benchmark.md` **[novo]** — relatório consolidado (qualidade pendente de avaliação humana)
- `docs/stories/5.6.harness-benchmark-llm.md` **[modificado]** — progresso da story

## QA Results

### Quality Gate — @architect, 2026-07-09

**Verdict: WAIVED** ⚖️ (todos os 6 ACs entregues; único item dispensado é a avaliação cega humana — waiver formal e documentado da equipe)

| Check | Resultado |
|---|---|
| Reúso do `llm.py` (5.1) — AC1 | ✅ 100% via `chat_stream()`; único ponto que varia é `config.insight.model`, restaurado em `finally` (nunca vaza override). Reusa `montar_contexto`/`limpar_label` (5.2) e `montar_prompt_rag`/`retrieve` (5.4/5.5) — zero cliente/prompt novo |
| Amostra congelada — AC2 | ✅ 12 tópicos (5 estratos: topo/anomalia/ruidoso/nicho/multi_tema) + 8 perguntas RAG (5 respondíveis + 3 pegadinhas), determinística |
| Medição latência/throughput/VRAM — AC3 | ✅ `resultados_brutos.parquet` com 96 linhas (32/modelo × 3); `ollama stop` entre modelos p/ VRAM justa; aproximação de tokens declarada |
| Planilha cega — AC4 | ✅ `avaliacao_cega.csv` 85 linhas avaliáveis, embaralhada por item (seed 42, determinístico e testado), SEM vazamento de modelo (verificado); vínculo só em `mapa_cego.json` |
| Relatório + recomendação — AC5 | ✅ `resultado-benchmark.md` com desempenho + confiabilidade + recomendação **manter `qwen2.5:14b`**; vencedor = default ⇒ sem troca de `insight.model`, sem alteração no ADR-002 |
| Limitações + degradação graciosa — AC6 | ✅ amostra pequena/Q4/aproximação de tokens declaradas; modelo ausente pulado com log; `nvidia-smi` ausente ⇒ VRAM vazia; falha vira `status=falha_llm`, nunca exceção fatal |
| Testes (pytest) | ✅ suíte completa **153 testes verdes** (22,6s); 11 novos cobrem amostra estratificada, embaralhamento cego determinístico, medição e regra de decisão |
| Lint (ruff) | ✅ `scripts/benchmark_llm.py` + `tests/test_benchmark_llm.py` limpos |
| Sem regressões | ✅ artefatos entram/saem do diretório de pesquisa (não de `dados/`); override de modelo isolado com restauração garantida |

**Motivo do WAIVED (não-bloqueante):**
- A **avaliação de qualidade textual humana** (pontuação cega ≥ 2 avaliadores) foi **formalmente dispensada por decisão da equipe** (Change Log 1.1) e está documentada com justificativa técnica no relatório (§Qualidade — "NÃO EXECUTADA"). Fundamentos: (1) `qwen3.5:9b` eliminado por **confiabilidade**, não qualidade (0% saída sob contrato de produção de 512 tokens; 34% falha mesmo com 8× o budget; ~17s até 1º token útil) — nenhuma nota reverteria; (2) entre os 2 finalistas 100% confiáveis, `qwen2.5:14b` vence todas as métricas medidas e ambos recusaram as 3 pegadinhas RAG (sinal parcial de fidelidade); (3) o vencedor coincide com o default do ADR-002 — a avaliação cega só justificaria uma *troca*. O AC4 em si (gerar a planilha cega) foi **entregue**; a planilha e o mapa permanecem no repo para execução a posteriori sem repetir medições.

**Observações não-bloqueantes (carry-forward):**
- Cosmético "[1]" ocasional após recusa (herdado da 5.5): o prompt de produção é insumo **congelado** do benchmark — alterá-lo agora invalidaria a comparação. Segue como ajuste fino pós-benchmark (fora de escopo desta story).
- Throughput é aproximado por nº de chunks do stream (~1 token/chunk no Ollama) — aproximação honesta e declarada no relatório; suficiente para o sinal comparativo pretendido (COULD/não-bloqueante).

**Segurança:** sem superfície nova relevante — script offline read-only sobre artefatos locais; override de `config.insight.model` isolado e restaurado em `finally`; nenhuma credencial ou entrada externa não confiável no caminho do harness.

---

> **Nota de processo (@sm):** ClickUp/code-intel não configurados → skip gracioso.
