# Story 3.1: Séries temporais por tópico

## Status

Ready

## Executor Assignment

```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [git, ruff, pytest]
```

> **Nota da equipe:** Dificuldade 🟡 (aprende com pareamento leve). Owner sugerido: Integrante D (fase Scores). Produz `series.parquet` (parte do **A4**).

## Story

**As a** cientista de dados do TrendRadar,
**I want** montar a série de frequência diária de cada tópico,
**so that** o crescimento de cada assunto possa ser medido ao longo do tempo.

## Acceptance Criteria

1. Para cada tópico, série de **contagem por dia** no período do corpus.
2. **Agregação semanal** disponível para tópicos esparsos (coluna `count_weekly` opcional).
3. Séries persistidas (`dados/scores/series.parquet`); dias sem menção tratados com **zero-fill**.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled — validação por revisão manual.

## Tasks / Subtasks

- [ ] **Task 1 — Carregar atribuições** (AC: 1)
  - [ ] Ler `dados/topics/doc_topics.parquet` (`{doc_id, data, topic_id}`).
- [ ] **Task 2 — Contar por dia** (AC: 1, 3)
  - [ ] Agrupar por `(topic_id, data)` e contar; reindexar para todos os dias do período (**zero-fill**).
- [ ] **Task 3 — Agregação semanal** (AC: 2)
  - [ ] Gerar `count_weekly` (resample semanal) para uso em tópicos esparsos.
- [ ] **Task 4 — Persistir** (AC: 3)
  - [ ] Salvar `dados/scores/series.parquet` (`{topic_id, data, count[, count_weekly]}`).

## 📘 Guia de Desenvolvimento (para o integrante)

> Dificuldade 🟡 — peça revisão ao final. Conceitos repetidos nos glossários anteriores.

### 🎯 O que você vai construir
Para cada assunto (tópico), um "gráfico de quantas notícias por dia" — uma linha do tempo. Essa série é a matéria-prima para medir se um assunto está crescendo (a próxima story calcula o score em cima disso).

### 🧠 Conceitos que você precisa entender
- **Série temporal:** uma sequência de valores ao longo do tempo (aqui: nº de notícias por dia, por tópico).
- **Zero-fill:** se num dia o tópico não teve nenhuma notícia, o valor daquele dia é **0** (não "faltando"). Isso é essencial — sem o zero, as contas de crescimento da próxima story dão erro.
- **Resample / agregação semanal:** "juntar" os dias em semanas. Útil para tópicos com poucas menções (esparsos), onde o dia-a-dia é muito ruidoso.

### 🪜 Passo a passo
1. **Carregue** (Task 1): leia `doc_topics.parquet`.
2. **Conte por dia** (Task 2): `groupby(["topic_id", "data"]).size()`. Depois **reindexe** cada tópico para o intervalo completo de datas do corpus, preenchendo dias faltantes com 0. *Por quê:* a série precisa ser contínua (todos os dias), senão "crescimento" e "z-score" calculam errado.
3. **Some por semana** (Task 3): use `resample("W")` para gerar `count_weekly`. *Por quê:* dá uma visão mais estável para tópicos com poucos dados.
4. **Salve** (Task 4): grave `series.parquet`.

### ✅ Como saber que funcionou
- `series.parquet` tem `{topic_id, data, count}` cobrindo **todos** os dias do período (com zeros). *(AC1, AC3)*
- Existe `count_weekly` para a visão semanal. *(AC2)*
- Para um tópico, somar `count` = nº de notícias daquele tópico no corpus.

### ⚠️ Erros comuns & soluções
- **Dias faltando (sem zero):** esqueceu o reindex/zero-fill — a próxima story vai quebrar. Confirme que todo dia do período existe.
- **Datas como texto:** garanta que `data` é tipo data (`pd.to_datetime`) antes de agrupar/resample.

### 🆘 Quando me chamar
- Para decidir o intervalo exato do período (início/fim) usado no reindex.

### 📚 Glossário (novos termos)
| Termo | Significado simples |
|---|---|
| Série temporal | Valores ao longo do tempo |
| Zero-fill | Preencher com 0 os dias sem dado |
| Resample | Reagrupar por outra granularidade (dia→semana) |

## Dev Notes

> Contexto de `docs/architecture.md` (v1.0 → Contratos A4) e `docs/design/trend-score.md` (§2).

- **Owner:** `src/scores/series.py`. Função: `montar_series(doc_topics) -> series`.
- **Entrada:** `doc_topics.parquet` (A3). **Saída:** `dados/scores/series.parquet` (parte do A4).
- **Schema:** `series = {topic_id, data, count[, count_weekly]}`, **zero-fill** nos dias sem menção.
- **Por que zero-fill importa:** a Camada 1 (Story 3.2) usa janelas `R`/`P` e `z-score` que assumem série contínua diária (spec §3).
- **Granularidade:** diária por padrão; semanal para esparsos (spec §2).

### Testing
- **Local:** `tests/test_series.py`.
- **Foco determinístico:** dado um conjunto pequeno de `{topic_id, data}`, a série sai com zero-fill correto e a soma bate com a contagem de entrada.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-09 | 0.1 | Draft inicial | River (@sm) |
| 2026-06-09 | 0.2 | Validated GO (8/10) — Status: Draft → Ready | @po |

## Dev Agent Record
### Agent Model Used
_(a preencher)_
### Debug Log References
_(a preencher)_
### Completion Notes List
_(a preencher)_
### File List
_(a preencher)_

## QA Results
_(a preencher)_
