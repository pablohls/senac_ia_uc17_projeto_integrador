# Story 1.1: Setup do projeto e estrutura

## Status

Done

## Executor Assignment

```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [git, ruff, pytest]
```

> **Nota da equipe (contexto acadêmico):** o "executor" formal é `@dev`, mas na prática esta story é feita **manualmente por um integrante** como exercício de aprendizado, com o integrante técnico no papel de `@architect` (revisor/desbloqueador). Dificuldade: 🟢 estrutura · 🔴 parte de GPU/CUDA (ponto de pareamento — ver guia).

## Story

**As a** equipe de desenvolvimento do SONAR,
**I want** a estrutura do repositório e o ambiente Python configurados com Poetry,
**so that** os 4 integrantes trabalhem sobre a mesma base modular e reprodutível, sem o problema de "só roda na máquina de um".

## Acceptance Criteria

1. Repositório contém a estrutura de pastas da arquitetura: `src/{common,coleta,pln,modelagem,scores,dashboard}/`, `config/`, `dados/{raw,processed,topics,scores}/`, `docs/`, `tests/`, `notebooks/`.
2. **Poetry** configurado (`pyproject.toml` + `poetry.lock`) com as dependências do MVP; `poetry install` conclui **sem erro**.
3. Um script de **smoke test** (`tests/smoke_test.py` ou `python -m src.common.smoke`) roda e confirma: (a) imports principais OK, (b) PyTorch enxerga a GPU (`torch.cuda.is_available()` → `True`, ou aviso explícito de modo CPU), (c) leitura/escrita Parquet OK.
4. `.gitignore` exclui artefatos pesados não versionados, `.venv`, `__pycache__`; `.gitattributes` configura **Git LFS** para `*.parquet` e `*.npy`; `git init` realizado e primeiro commit feito.
5. `config/config.yaml` existe com os parâmetros-base (fontes, `w`, `α`, `H`, `λ`, `n_min`, `k`) e é carregado/validado por `src/common/config.py` (pydantic) sem erro.
6. `README.md` documenta os 3 comandos de reprodução do demo (`poetry install` → `poetry run sonar` → `streamlit run`).

> **Reconciliação PRD × Arquitetura:** o PRD original (AC2/AC4) citava `requirements.txt`/`environment.yml`. A arquitetura (v1.0) decidiu **Poetry** e ampliou a estrutura com `src/common` e `src/scores`. Esta story segue a **arquitetura** (fonte mais recente). Mudança registrada no Change Log.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI não está habilitado em `core-config.yaml`. A validação de qualidade usará **revisão manual** (integrante técnico no papel de `@architect`/`@qa`).

## Tasks / Subtasks

- [x] **Task 1 — Esqueleto de pastas** (AC: 1)
  - [x] Criar as pastas `src/{common,coleta,pln,modelagem,scores,dashboard}/` com um `__init__.py` vazio em cada.
  - [x] Criar `config/`, `dados/{raw,processed,topics,scores}/`, `tests/`, `notebooks/`.
  - [x] Colocar um `.gitkeep` em cada subpasta de `dados/` (pra elas existirem no git mesmo vazias).
- [x] **Task 2 — Poetry e dependências** (AC: 2)
  - [x] `poetry init` (ou criar `pyproject.toml` a partir do modelo da arquitetura).
  - [x] Adicionar a source `pytorch-cu124` com `priority=explicit` e `torch` apontando para ela.
  - [x] Adicionar as demais deps do MVP (ver Dev Notes) e o grupo `dev` (pytest, ruff).
  - [x] Rodar `poetry install` e confirmar sucesso; commitar `poetry.lock`.
- [x] **Task 3 — Config central** (AC: 5)
  - [x] Criar `config/config.yaml` com os parâmetros-base.
  - [x] Criar `src/common/config.py` com um modelo pydantic que carrega e valida o YAML.
- [x] **Task 4 — Smoke test** (AC: 3)
  - [x] Criar `tests/smoke_test.py` que checa imports, GPU e round-trip de Parquet.
  - [x] Rodar `poetry run pytest tests/smoke_test.py` e confirmar verde.
- [x] **Task 5 — Git + LFS + README** (AC: 4, 6)
  - [x] Criar `.gitignore` e `.gitattributes` (Git LFS para `*.parquet`, `*.npy`).
  - [x] `git init`, `git lfs install`, primeiro commit.
  - [x] Escrever o `README.md` com os 3 comandos de reprodução.

## 📘 Guia de Desenvolvimento (para o integrante)

> Este guia é para quem vai **fazer esta story manualmente para aprender**. Vá com calma e leia o "porquê" de cada passo — é aí que está o aprendizado. Dificuldade geral: 🟢, **exceto** a parte de GPU/CUDA (🔴) — nesse ponto, chame o integrante técnico.

### 🎯 O que você vai construir
O "esqueleto" do projeto: as pastas onde cada parte vai morar, o ambiente que instala todas as ferramentas que vamos usar, e um pequeno teste que confirma "está tudo no lugar e funcionando". Nada de lógica do produto ainda — só a fundação. É como preparar a obra (terreno, fundação, ferramentas) antes de levantar as paredes.

### 🧠 Conceitos que você precisa entender
- **Repositório / Git:** uma "pasta com histórico". Toda mudança fica registrada e a equipe compartilha o mesmo conteúdo. (Busque: "o que é git e github em 5 minutos".)
- **Ambiente virtual / Poetry:** cada projeto Python precisa de um conjunto isolado de "bibliotecas" (ferramentas). O **Poetry** instala essas bibliotecas em versões exatas e travadas, garantindo que a mesma coisa rode na máquina de todos. (Busque: "poetry python para iniciantes".)
- **Dependências (`pyproject.toml` / `poetry.lock`):** a "lista de compras" das bibliotecas (`pyproject.toml`) e a "nota fiscal com as versões exatas que foram instaladas" (`poetry.lock`). O `lock` é o que evita o "na minha máquina funciona".
- **GPU / CUDA:** a placa de vídeo acelera o aprendizado de máquina. **CUDA** é o "tradutor" entre o Python e a placa NVIDIA. A versão do CUDA precisa combinar com o driver da placa — por isso essa parte é delicada.
- **Parquet:** um formato de tabela (como um Excel, mas rápido e tipado) que vamos usar para passar dados de uma fase para outra.

### 🪜 Passo a passo

**1. Crie a estrutura de pastas** (Task 1)
No terminal, dentro da pasta do projeto, crie as pastas listadas no AC1. Em cada pasta dentro de `src/`, crie um arquivo vazio chamado `__init__.py` — ele avisa o Python que aquela pasta é um "módulo" importável.
*Por quê:* separar por fase (`coleta`, `pln`, ...) é o que permite os 4 trabalharem sem pisar no código um do outro.

**2. Inicie o Poetry e adicione as dependências** (Task 2)
- Instale o Poetry (se ainda não tiver): siga o site oficial.
- Use o modelo de `pyproject.toml` que está em `docs/architecture.md` (seção "Environment com Poetry") — ele já tem a lista de bibliotecas e o truque do PyTorch.
- ⚠️ **PARE AQUI e chame o integrante técnico** para a parte do `torch`/`pytorch-cu124`: a versão `cu124` precisa bater com o driver da GPU de vocês. Se não bater, troca-se uma palavra (`cu121`, `cpu`), mas é preciso conferir junto.
- Rode `poetry install`. Pode demorar (baixa bibliotecas grandes). Se terminar sem mensagem em vermelho, deu certo.
*Por quê:* o `poetry install` cria o ambiente isolado e trava as versões no `poetry.lock`, que você vai commitar — é o coração da reprodutibilidade.

**3. Crie a configuração central** (Task 3)
- Crie `config/config.yaml` copiando os parâmetros da tabela do ADR-001 (`w=7`, `α=1`, `H=60`, `λ=1.0`, `n_min=5`, `k=2.5`) + a lista de fontes.
- Crie `src/common/config.py` com uma classe pydantic que lê esse YAML. (Há um exemplo de padrão nas Dev Notes abaixo.)
*Por quê:* deixar todos os "números mágicos" num só lugar é regra do projeto (Coding Standards) — a banca valoriza poder ajustar e justificar parâmetros.

**4. Escreva o smoke test** (Task 4)
- Crie `tests/smoke_test.py` com 3 verificações: (a) `import torch, pandas, sentence_transformers` funciona; (b) imprime se a GPU foi encontrada; (c) salva um DataFrame de teste em `.parquet` e lê de volta.
- Rode `poetry run pytest tests/smoke_test.py`.
*Por quê:* este teste é o "checklist de obra" — antes de qualquer um começar a fase dele, roda isso e sabe que o ambiente está saudável.

**5. Git, LFS e README** (Task 5)
- Crie `.gitignore` (ignora `.venv/`, `__pycache__/`, dados temporários) e `.gitattributes` (manda o Git LFS cuidar de `*.parquet` e `*.npy`, que são pesados).
- Rode `git init`, depois `git lfs install`, e faça o primeiro commit.
- Escreva o `README.md` com os 3 comandos de reprodução (estão no AC6).
*Por quê:* o LFS evita encher o repositório de arquivos gigantes; o README é o que garante que a banca (ou um integrante novo) consiga rodar o projeto do zero.

### ✅ Como saber que funcionou
- `poetry install` terminou sem erro e existe um `poetry.lock`. *(AC2)*
- `poetry run pytest tests/smoke_test.py` ficou **verde** e mostrou a linha da GPU. *(AC3)*
- `git status` mostra o projeto versionado; o primeiro commit aparece em `git log`. *(AC4)*
- Abrir `config/config.yaml` e rodar um pequeno teste de carga do `config.py` não dá erro. *(AC5)*

### ⚠️ Erros comuns & soluções
- **`poetry install` travando/lento na resolução do `torch`:** quase sempre é o `priority = "explicit"` faltando na source do PyTorch. Confira essa linha.
- **`torch.cuda.is_available()` retorna `False`:** driver/CUDA incompatível. Não trave o projeto por isso — registre como "modo CPU" e siga; é ponto de pareamento com o técnico.
- **Git reclamando do tamanho de arquivos:** faltou configurar o LFS antes de commitar os `.parquet`/`.npy`. Rode `git lfs install` e confira o `.gitattributes`.
- **`ModuleNotFoundError` ao importar `src.xxx`:** faltou o `__init__.py` na pasta, ou você não está rodando com `poetry run`.

### 🆘 Quando parar e me chamar (integrante técnico)
- Na **configuração do PyTorch/CUDA** (Passo 2) — sempre.
- Se o `poetry install` falhar com erro em vermelho que você não entende.
- Se o smoke test acusar erro que não está na lista de "erros comuns".

### 📚 Glossário
| Termo | Significado simples |
|---|---|
| Módulo | Pasta de código Python importável (tem `__init__.py`) |
| Dependência | Biblioteca/ferramenta que o projeto usa |
| Commit | "Salvar com histórico" uma mudança no Git |
| LFS | Large File Storage — Git para arquivos grandes |
| Smoke test | Teste rápido de "está tudo de pé?" |
| pydantic | Biblioteca que valida se a configuração está no formato certo |

## Dev Notes

> Contexto extraído de `docs/architecture.md` (v1.0) e `docs/prd.md` (v1.1) — o dev/integrante **não precisa abrir** esses arquivos para esta story.

**Estrutura-alvo do projeto** (de architecture.md → Unified Project Structure):
```
sonar/
├── config/config.yaml
├── src/{common,coleta,pln,modelagem,scores,dashboard}/
├── dados/{raw,processed,topics,scores}/
├── tests/  notebooks/  docs/
├── pyproject.toml  poetry.lock
├── .gitignore  .gitattributes  README.md  run_all.py
```

**Dependências do MVP** (de architecture.md → Tech Stack):
`python ^3.12`, `torch ^2.4` (source `pytorch-cu124`), `sentence-transformers ^3.0`, `bertopic ^0.16`, `pandas ^2.2`, `pyarrow ^17.0`, `numpy`, `statsmodels ^0.14`, `streamlit ^1.38`, `plotly ^5.22`, `networkx ^3.3`, `pydantic ^2.8`, `pyyaml ^6.0`, `trafilatura ^1.12`, `lxml ^5.2`, `requests ^2.32`. Grupo dev: `pytest ^8.3`, `ruff ^0.6`.

**Bloco da source PyTorch** (obrigatório no `pyproject.toml`):
```toml
[[tool.poetry.source]]
name = "pytorch-cu124"
url = "https://download.pytorch.org/whl/cu124"
priority = "explicit"
```
E `torch = { version = "^2.4", source = "pytorch-cu124" }`. Fallback: `cu121` / `cpu` conforme driver da GPU (should-fix levantado no architect-checklist).

**Padrão do `src/common/config.py`** (pydantic):
```python
from pydantic import BaseModel
import yaml

class TrendScoreParams(BaseModel):
    w: int = 7; alpha: float = 1.0; H: int = 60
    lambda_burst: float = 1.0; n_min: int = 5; k: float = 2.5

class Config(BaseModel):
    fontes: list[str]
    embedding_model: str
    trend_score: TrendScoreParams

def load_config(path: str = "config/config.yaml") -> Config:
    with open(path, encoding="utf-8") as f:
        return Config(**yaml.safe_load(f))
```

**Coding Standards aplicáveis** (de architecture.md): config só via objeto pydantic (sem número mágico); imports absolutos a partir de `src/`; `ruff` para lint/format.

**Script Poetry** a registrar no `pyproject.toml` (usado por stories futuras): `[tool.poetry.scripts]` → `sonar = "run_all:main"` (o `run_all.py` será implementado em story posterior; nesta story basta um stub `def main(): ...`).

### Testing

- **Local dos testes:** `tests/` na raiz.
- **Framework:** `pytest` (rodar com `poetry run pytest`).
- **Para esta story:** `tests/smoke_test.py` com 3 asserts — imports OK, GPU detectada (ou aviso de CPU, sem falhar o teste), round-trip Parquet (`df.to_parquet` → `pd.read_parquet` retorna o mesmo conteúdo).
- **Padrão:** testes determinísticos; nenhuma dependência de rede nesta story.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-09 | 0.1 | Draft inicial (piloto de formato com Guia de Desenvolvimento). Reconciliação PRD→Arquitetura: Poetry no lugar de requirements.txt; estrutura ampliada com `src/common` e `src/scores` | River (@sm) |
| 2026-06-09 | 0.2 | Validated GO (9/10) — Status: Draft → Ready | @po |
| 2026-06-10 | 0.3 | Development started (interactive mode) — Status: Ready → InProgress | @dev |
| 2026-06-10 | 0.4 | Implementação concluída (6/6 AC; smoke test 4/4, GPU T1000 OK; ruff verde). Reconciliações: requirements.txt→Poetry, .gitignore vs LFS, pyproject→PEP 621. Blocker de disco C: cheio resolvido (cache→D:). Status: InProgress → InReview | @dev |
| 2026-06-10 | 0.5 | QA gate PASS (quality score 100/100; 6/6 AC rastreados; NFRs todos PASS). Status: InReview → Done | @qa |

## Dev Agent Record

### Agent Model Used
claude-opus-4-8[1m] (Opus 4.8, 1M context) — @dev (Dex), modo interactive.

### Debug Log References
- **Blocker resolvido — `[Errno 28] No space left on device` no `poetry install`:** o disco `C:` estava 100% cheio (2,4 GB livres) e o wheel `torch 2.6.0+cu124` (~2,5 GB) baixava para o cache em `C:\Users\...\AppData\Local\pypoetry\Cache`. **Correção:** `poetry config cache-dir D:\poetry-cache` + `TMP/TEMP` → `D:\poetry-tmp` (`D:` tem 757 GB livres). Reinstalado com sucesso (exit 0, 92 pacotes).
- Smoke test: `4 passed in 60.52s` — GPU detectada (`NVIDIA T1000`, `torch.cuda.is_available()=True`).
- Lint: `ruff check` + `ruff format` → "All checks passed!" / "11 files already formatted".

### Completion Notes List
- **Ambiente validado:** Python 3.12.7, Poetry 2.3.1, Git 2.52, Git LFS 3.7.1, GPU NVIDIA T1000 (driver 573.44 → CUDA 12.4 compatível, **sem fallback necessário**).
- **PyTorch/CUDA (ponto 🔴 da story):** `pytorch-cu124` instalou e a GPU foi reconhecida no smoke test. `torch.cuda.is_available()` retornou `True`.
- **`pyproject.toml`** migrado para o formato moderno PEP 621 (`[project]`) por causa do Poetry 2.x (eliminou warnings de deprecação); deps em `[tool.poetry.dependencies]` via `dynamic = ["dependencies"]` para preservar a source explícita do `torch`.
- **Reconciliações aplicadas (documentadas no Change Log):** `requirements.txt` removido (substituído por Poetry, conforme decisão da arquitetura); `.gitignore` ajustado — `*.parquet` retirado do ignore global (anulava o Git LFS do AC4), mantendo `dados/` e modelos pesados (`*.pt`, `*.bin`, `models/`) ignorados.
- **`README.md`** atualizado com os 3 comandos de reprodução (AC6), preservando visão geral e seção de ética/LGPD.
- **`src/coleta/backfill_olhardigital.py`** (prova de conceito pré-existente, fora do escopo) recebeu auto-fix do `ruff` (`UP017` + format) apenas para deixar o gate de lint verde — sem mudança funcional.
- **CodeRabbit:** Disabled em `core-config.yaml` → self-healing loop pulado; validação de qualidade fica para revisão manual (@architect/@qa), conforme a própria story.
- **Pendente (não bloqueante):** `git commit` local dos arquivos desta story; push/PR via @devops.

### File List
**Criados:**
- `pyproject.toml`, `poetry.lock` (gerado), `run_all.py` (stub do CLI `sonar`)
- `.gitattributes` (Git LFS: `*.parquet`, `*.npy`)
- `config/config.yaml`
- `src/__init__.py`, `src/common/__init__.py`, `src/coleta/__init__.py`, `src/pln/__init__.py`, `src/modelagem/__init__.py`, `src/scores/__init__.py`, `src/dashboard/__init__.py`
- `src/common/config.py`
- `tests/smoke_test.py`
- `dados/raw/.gitkeep`, `dados/processed/.gitkeep`, `dados/topics/.gitkeep`, `dados/scores/.gitkeep`

**Modificados:**
- `.gitignore` (ajuste LFS vs ignore)
- `README.md` (comandos de reprodução AC6)
- `src/coleta/backfill_olhardigital.py` (ruff auto-fix; pré-existente)

**Removidos:**
- `requirements.txt` (substituído por Poetry)

## QA Results

### Review Date: 2026-06-10

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment
Implementação sólida e bem documentada para uma story de fundação. Verificação **independente** (não apenas confiando no handoff): testes re-executados (`4 passed`), `ruff check` limpo, atributos de LFS, `.gitkeep` e `.gitignore` conferidos via git. O código de `src/common/config.py` usa pydantic com `Field` constraints, type hints e docstrings; `config/config.yaml` está bem comentado (ótimo para a banca). `pyproject.toml` foi corretamente migrado para PEP 621 mantendo a source explícita do `torch`. Nenhum refactoring necessário.

### Refactoring Performed
Nenhum. O código já atende aos padrões; alterações seriam cosméticas.

### Compliance Check
- Coding Standards: ✓ (config centralizada sem números mágicos; imports absolutos; `ruff` verde)
- Project Structure: ✓ (estrutura bate com a arquitetura v1.0: `src/{common,coleta,pln,modelagem,scores,dashboard}`)
- Testing Strategy: ✓ (`tests/smoke_test.py` com 4 asserts determinísticos; GPU não falha em CPU)
- All ACs Met: ✓ (6/6 — ver matriz de rastreabilidade abaixo)

### Requirements Traceability (Given-When-Then)
- **AC1** Estrutura: *Given* o repo, *when* listo `src/`/`dados/`, *then* todas as 6+ pastas e `.gitkeep` existem. ✓ (verificado via `git ls-files`)
- **AC2** Poetry: *Given* `pyproject.toml`, *when* rodo `poetry install`, *then* conclui exit 0 com `poetry.lock`. ✓
- **AC3** Smoke test: *Given* o ambiente, *when* `pytest tests/smoke_test.py`, *then* imports OK + GPU `NVIDIA T1000` detectada + round-trip Parquet. ✓ (4/4)
- **AC4** Git/LFS: *Given* `.gitattributes`, *when* `git check-attr`, *then* `*.parquet`/`*.npy` → `filter: lfs`; `.gitkeep` versionados; CSV pesado ignorado; commit `dd0216c`. ✓
- **AC5** Config: *Given* `config.yaml`, *when* `load_config()`, *then* objeto pydantic válido (testado no smoke). ✓
- **AC6** README: *Given* o README, *when* leio "Como rodar", *then* os 3 comandos de reprodução presentes. ✓

### Security Review
✓ Sem problemas. `.env` ignorado (apenas `.env.example` versionado); nenhum secret hardcoded em código/config; sem PII nesta fase. Coleta de dados respeita ética/LGPD conforme README.

### Performance Considerations
N/A para setup. Smoke test ~38–60s (carga do torch). Sem gargalos.

### Improvements Checklist
- [ ] (future) Validar carregamento real do `embedding_model` na Story 2.2 (fora do smoke para não depender de rede)
- [ ] (future) Anotar no README que o passo 3 (`streamlit run`) fica disponível ao fim do Epic 4 (app.py vem na 4.1)

### Files Modified During Review
Nenhum (revisão não-invasiva; QA não commita nem altera File List).

### Gate Status
Gate: **PASS** → docs/qa/gates/1.1-setup-projeto-estrutura.yml
Quality Score: 100/100 · NFRs: security/performance/reliability/maintainability todos PASS

### Recommended Status
✓ **Ready for Done** — nenhuma mudança obrigatória; as 2 observações são evolução futura (não-bloqueantes). Decisão final do owner; push via @devops.
