# Story 1.5: Coleta incremental (agregação sem perda)

## Status

Done

## Executor Assignment

```
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [git, pytest]
```

> **Nota de processo:** story curta criada pelo @dev em fluxo abreviado, com
> autorização direta do owner (Pablo) durante a sessão de 2026-07-06 — a
> pergunta "se eu recoletar amanhã, perco os dados?" revelou que o `extract`
> **sobrescrevia** o corpus. Formalizada aqui para rastreabilidade; @sm/@po
> podem refinar a posteriori.

## Story

**As a** integrante da equipe rodando coletas recorrentes,
**I want** que novas coletas agreguem apenas os artigos novos ao corpus existente,
**so that** o corpus só cresça (nunca regrida) e recoletas diárias custem minutos, não horas.

## Acceptance Criteria

1. Rodar `extract` com um corpus existente **preserva** os artigos anteriores e baixa **apenas** URLs novas (dedup por `url`/`doc_id` estável).
2. Flag `--completo` força a reconstrução total (comportamento antigo, explícito).
3. O mesmo filtro incremental vale para a segunda fonte (`canaltech`), que já mesclava mas re-baixava tudo.
4. Testes determinísticos cobrindo: filtro de URLs conhecidas, corpus vazio/ausente, recoleta sem novidade e agregação sem perda.

## Tasks / Subtasks

- [x] **Task 1 — Filtro incremental** (AC: 1)
  - [x] `filtrar_urls_novas(urls_df, corpus_atual)` em `src/coleta/extract.py` (pura, testável).
- [x] **Task 2 — Fluxo do extract** (AC: 1, 2)
  - [x] `main()` lê o corpus existente, filtra novas, extrai só as novas e mescla via `mesclar_corpus`.
  - [x] `--completo` para reconstrução total; `main(argv)` para não herdar args do CLI `sonar`.
- [x] **Task 3 — Canaltech incremental** (AC: 3)
  - [x] Filtrar URLs contra o corpus antes de extrair.
- [x] **Task 4 — Testes** (AC: 4)
  - [x] 4 novos testes em `tests/test_extract.py`.

## Dev Notes

- O `doc_id = SHA-1(url)[:16]` imutável (contrato A1) torna o dedup incremental seguro.
- Granularidade da janela segue mensal (`coleta.*.meses`); o custo de re-listar o
  sitemap é baixo (só XML) — o ganho está em não re-BAIXAR artigos já extraídos.
- Manifesto transversal registra `extract.incremental: true` e `stage_version: 1.5`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-06 | 1.0 | Implementação completa (fluxo abreviado autorizado pelo owner). Status: → InReview | @dev |
| 2026-07-09 | 1.1 | Quality gate formal: PASS (7/7 checks, 4/4 ACs). Status: InReview → Done | @architect |

## Registro de Implementação
### Ambiente e Proveniência
@dev
### Debug Log References
`poetry run pytest tests/test_extract.py` — 18 passed
### Completion Notes List
- Incremental é o DEFAULT quando o corpus existe (princípio do menor susto para
  recoletas do dia a dia); reconstrução total é opt-in via `--completo`
- `extract.main(argv=[])` no run_all evita que o argparse herde `--com-coleta`
### File List
- src/coleta/extract.py (filtrar_urls_novas + main incremental)
- src/coleta/canaltech.py (filtro incremental)
- run_all.py (argv explícito)
- tests/test_extract.py (4 testes novos)
- README.md (nota de coleta incremental)

## QA Results

**Quality Gate — @architect — 2026-07-09**

**Veredito: PASS** — 7/7 checks aprovados; 4/4 ACs atendidos.

### 7 Quality Checks

| # | Check | Resultado | Evidência |
|---|-------|-----------|-----------|
| 1 | Code review | PASS | `filtrar_urls_novas(urls_df, corpus_atual)` é função **pura**, sem rede/I/O, determinística, não muta a entrada (`isin` + `reset_index(drop=True)`); trata `None`/vazio como "tudo novo"; docstrings de contrato claras. `main(argv)` explícito evita herança de args do CLI pai. Reuso limpo de `mesclar_corpus` (dedup por url, keep first). Zero acoplamento novo. |
| 2 | Unit tests | PASS | `tests/test_extract.py` → **18 passed** (14 pré-existentes 1.3/1.4 + 4 novos da 1.5): filtro de URLs conhecidas, corpus vazio/ausente (`None` e `DataFrame` vazio), recoleta sem novidade (0 URLs), agregação sem perda. Todos determinísticos, sem rede. |
| 3 | Acceptance criteria | PASS | 4/4 ACs verificados contra código real (ver tabela abaixo). |
| 4 | No regressions | PASS | `poetry run pytest tests/ -q` → **172 passed** em 30s, zero regressão. Fases 5 e 6 (Done) foram construídas sobre este fluxo e permanecem verdes. |
| 5 | Performance | PASS | Este é o próprio ganho de performance da story: recoleta filtra URLs já extraídas **antes** de qualquer download → só XML do sitemap é re-listado (barato), artigos existentes não são re-baixados. `filtrar_urls_novas` usa `set` lookup (O(1) por URL). Reduz também carga sobre os portais-alvo (menos requests, respeita NFR4/rate-limit). |
| 6 | Security | PASS | Zero dependência nova (`pyproject.toml`/`poetry.lock` intocados). Incremental **diminui** a superfície de rede (menos requisições aos portais); `robots.txt` + rate-limit da Story 1.3 preservados. `doc_id = SHA-1(url)[:16]` imutável mantém dedup seguro entre execuções. Sem input externo não-validado. |
| 7 | Documentation | PASS | Docstrings de `filtrar_urls_novas` e `main(argv)` explicam o contrato incremental e o motivo do `argv=[]`; comentário em `canaltech.py` marca o filtro incremental; README com nota de coleta incremental (File List). Manifesto registra `extract.incremental` e `stage_version`. |

### Verificação dos 4 ACs

- **AC1 (incremental preserva + só baixa novas, dedup por url/doc_id):** `main()` em `src/coleta/extract.py` — modo incremental é o **default** quando `CAMINHO_CORPUS` existe: lê o corpus, `filtrar_urls_novas` remove URLs já conhecidas, extrai só as novas e `mesclar_corpus(atual, novos)` agrega (dedup por `url`, keep first). `doc_id` derivado só da URL garante estabilidade. Testes `test_filtrar_urls_novas_remove_conhecidas` + `test_incremental_agrega_sem_perder_dados`. **OK**.
- **AC2 (`--completo` força reconstrução total):** flag `--completo` (argparse) → `atual = None` → `filtrar_urls_novas` não roda, todas as URLs são extraídas e `mesclar_corpus(None, ...)` reconstrói do zero; `stage_version` volta a `1.3`. **OK**.
- **AC3 (mesmo filtro na 2ª fonte / canaltech):** `src/coleta/canaltech.py` lê o corpus atual e aplica `filtrar_urls_novas(urls, atual)` **antes** de `extrair_artigos` — antes mesclava mas re-baixava tudo; agora só baixa o que falta. **OK**.
- **AC4 (testes determinísticos: filtro, vazio/ausente, recoleta sem novidade, agregação sem perda):** 4 testes novos em `tests/test_extract.py` cobrem exatamente os quatro cenários; corpus ausente coberto por `None` **e** `DataFrame` vazio. **OK**.

### Issues

Nenhum issue de severidade alta/média/baixa. Implementação limpa (funções puras testáveis), escopo respeitado, dedup seguro pelo contrato do `doc_id`.

### Observações (não-bloqueantes)

- **[low/docs]** `canaltech.py` mantém `stage_version="1.4"` no manifesto e não registra `extract.incremental` (diferente do `extract.py`, que grava `1.5` + `extract.incremental`). O comportamento incremental está correto e testado; apenas o carimbo do manifesto da 2ª fonte é menos rico. Cosmético — não afeta AC nem pipeline.
- **[info]** Por design, o filtro é por presença de URL: se o **conteúdo** de uma URL já coletada mudar, o incremental não re-baixa (correto para o caso de uso — recoletas diárias baratas). `--completo` é o opt-in explícito para forçar re-extração. Trade-off documentado nas Dev Notes.
- **[info]** `dados/run_manifest.json` aparece modificado na working tree — runtime alheio a esta story, fora do escopo do gate (não commitado por este agente).

**Status: InReview → Done.**
