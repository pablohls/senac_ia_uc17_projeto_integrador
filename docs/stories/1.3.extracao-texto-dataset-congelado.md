# Story 1.3: Extração de texto e dataset congelado

## Status

Ready

## Executor Assignment

```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [git, ruff, pytest]
```

> **Nota da equipe:** Dificuldade 🟡 (aprende com pareamento leve). Owner sugerido: Integrante A. Produz o **artefato A1** (`corpus.parquet`) — base de todas as fases seguintes.

## Story

**As a** analista de dados do TrendRadar,
**I want** raspar o texto limpo de cada artigo e salvar o dataset,
**so that** o corpus fique pronto e reprodutível offline (o "dataset congelado" do demo).

## Acceptance Criteria

1. Extração de `título` e `texto` de cada URL com `trafilatura`, respeitando rate-limit (~1 req/s) e `robots.txt`.
2. Falhas de extração são **logadas e não interrompem o lote** (artigo problemático é pulado).
3. Dataset salvo em **Parquet** (`dados/raw/corpus.parquet`) com colunas `doc_id, data, titulo, texto, fonte, categoria, url`, deduplicado por `url`.
4. Dataset **congelado** versionado (Git LFS) e documentado para uso pelas fases seguintes (demo offline).

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled — validação por revisão manual.

## Tasks / Subtasks

- [ ] **Task 1 — Ler a lista de URLs** (AC: 1)
  - [ ] Carregar `dados/raw/urls.parquet` (saída da Story 1.2) via `src/common/io.py`.
- [ ] **Task 2 — Extrair texto** (AC: 1, 2)
  - [ ] Para cada URL, baixar a página e extrair `titulo`+`texto` com `trafilatura`.
  - [ ] Aplicar rate-limit (~1 req/s) e `User-Agent`.
  - [ ] Envolver cada extração em try/except: falha → log + pula (não aborta o lote).
- [ ] **Task 3 — Montar o dataset A1** (AC: 3)
  - [ ] Gerar `doc_id` = hash estável da `url`.
  - [ ] Montar DataFrame no schema A1; deduplicar por `url`; descartar `texto` vazio.
- [ ] **Task 4 — Congelar e versionar** (AC: 3, 4)
  - [ ] Salvar `dados/raw/corpus.parquet` (via `io.py`, atualiza `run_manifest.json`).
  - [ ] Confirmar que o LFS está rastreando o arquivo; commitar.

## 📘 Guia de Desenvolvimento (para o integrante)

> Dificuldade 🟡 — peça revisão do técnico ao final. Conceitos repetidos no glossário da 1.1/1.2.

### 🎯 O que você vai construir
Agora que temos a **lista de endereços** (Story 1.2), vamos **visitar cada link e copiar o conteúdo** (título e corpo da notícia), montando a tabela final de notícias — o **"dataset congelado"**. "Congelado" porque, depois de pronto, ele não muda: o demo inteiro roda em cima dele, sem internet.

### 🧠 Conceitos que você precisa entender
- **Web scraping / extração:** programar o computador para "ler" uma página da web e tirar só o que interessa (o texto da notícia), ignorando menus, anúncios e rodapé. A biblioteca `trafilatura` faz o trabalho pesado.
- **`doc_id` (hash da URL):** um "código de barras" único e fixo para cada artigo, gerado a partir do link. **É a chave mais importante do projeto** — ela vai conectar este artigo às suas fases seguintes (embedding, tópico, série). Nunca pode mudar.
- **Tratamento de erro (try/except):** alguns links vão falhar (página fora do ar, formato estranho). Em vez de o programa todo parar, a gente "tenta; se der erro, anota e segue".
- **Rate-limit:** esperar ~1 segundo entre downloads, para não sobrecarregar o servidor do portal (é ética e respeito ao `robots.txt` — NFR4).

### 🪜 Passo a passo
1. **Carregue a lista** (Task 1): leia `dados/raw/urls.parquet`. *Por quê:* é o resultado da story anterior — não recolete URLs.
2. **Visite e extraia** (Task 2): para cada URL, use `trafilatura.fetch_url` + `trafilatura.extract` para pegar título e texto. Coloque um `time.sleep(1)` entre as visitas. *Por quê:* o sleep respeita o servidor; sem ele você pode ser bloqueado.
3. **Proteja o lote** (Task 2): coloque cada extração dentro de `try/except`. No `except`, registre no log (`logging`) e continue. *Por quê:* com milhares de artigos, alguns sempre falham — não pode derrubar tudo.
4. **Gere o `doc_id`** (Task 3): algo como `hashlib.sha1(url.encode()).hexdigest()[:16]`. *Por quê:* é o código único que atravessa todo o pipeline.
5. **Monte e limpe** (Task 3): junte tudo num DataFrame com as 7 colunas do schema A1; remova duplicatas e textos vazios.
6. **Salve e versione** (Task 4): grave `dados/raw/corpus.parquet` pelo `io.py` e confirme que o Git LFS está cuidando dele.

### ✅ Como saber que funcionou
- Existe `dados/raw/corpus.parquet` com as colunas `doc_id, data, titulo, texto, fonte, categoria, url`. *(AC3)*
- Nenhuma linha tem `texto` vazio; nenhuma `url` repetida. *(AC3)*
- O log mostra quantos artigos foram pulados (e o lote terminou mesmo com falhas). *(AC2)*
- O arquivo aparece como "LFS" no `git status`. *(AC4)*

### ⚠️ Erros comuns & soluções
- **Muito lento:** normal — são milhares de páginas a 1/s. Rode em janela tranquila; dá para salvar progresso parcial.
- **Texto vindo "sujo" (menus/anúncios):** confira se está usando `trafilatura.extract` (que limpa) e não pegando o HTML cru.
- **`doc_id` mudando entre execuções:** você gerou o hash de algo que muda (ex.: data de coleta). Gere **só a partir da `url`**.
- **Lote parando no meio:** faltou o try/except em alguma parte; envolva toda a extração.

### 🆘 Quando me chamar
- Se a taxa de falha de extração for alta (> ~20%) — pode ser bloqueio ou mudança no portal.
- Para revisar o schema final antes de "congelar" (este artefato alimenta todo o resto).

### 📚 Glossário (novos termos)
| Termo | Significado simples |
|---|---|
| Scraping | Extrair conteúdo de páginas web automaticamente |
| Hash | "Código de barras" gerado a partir de um texto |
| try/except | "Tente fazer; se der erro, faça outra coisa" |
| Log | Registro do que aconteceu (sucessos e falhas) |

## Dev Notes

> Contexto de `docs/architecture.md` (v1.0 → Contratos de Artefatos A1) e `docs/prd.md`.

- **Owner do módulo:** `src/coleta/extract.py`. Função: `extrair_artigos(urls_df, config) -> DataFrame (A1)`.
- **Entrada:** `dados/raw/urls.parquet` (Story 1.2). **Saída:** `dados/raw/corpus.parquet` (**artefato A1**).
- **Schema A1 (lei):** `doc_id: str (hash da url)` **[chave primária]**, `data: date`, `titulo: str`, `texto: str`, `fonte: category`, `categoria: str|null`, `url: str`.
- **Garantias do contrato:** deduplicado por `url`; `texto` não-vazio; falhas logadas e excluídas.
- **Coding Standards (críticos):** `doc_id` gerado **só da URL** e **imutável**; I/O via `src/common/io.py`; rate-limit/robots em `src/coleta`.
- **NFR4:** sem PII; respeita ToS/robots; rate-limit ~1 req/s.

### Testing
- **Local:** `tests/test_extract.py`.
- **Foco determinístico:** função de geração de `doc_id` (mesma URL → mesmo id) e a montagem/dedup do DataFrame a partir de uma lista fixa de artigos simulados. Extração real (rede) validada manualmente em amostra.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-09 | 0.1 | Draft inicial | River (@sm) |
| 2026-06-09 | 0.2 | Validated GO (9/10) — Status: Draft → Ready | @po |

## Dev Agent Record
### Agent Model Used
_(a preencher)_
### Debug Log References
_(a preencher)_
### Completion Notes List
_(a preencher)_
### File List
_(a preencher)_

## QA Results
_(a preencher)_
