# Story 5.2: Analista IA — briefing e rótulos por tópico (batch) [MUST]

## Status

Done

## Executor Assignment

```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [git, ruff, pytest]
```

> **Nota da equipe:** Dificuldade 🟡 (**intermediária** — orquestração + engenharia de prompt, sem treino de modelo). Prioridade **MUST** (é o **primeiro "uau" visível** da Fase 5). **Depende da Story 5.1** (cliente `chat()`). Referência: `docs/architecture/adr-002-camada-llm.md` (§Feature A, §Contrato A5), `docs/architecture/recommended-approach.md` (§3, §5A).

## Story

**As a** usuário do SONAR,
**I want** que cada tópico em ascensão ganhe um **nome em linguagem natural** e um parágrafo explicando **por que está subindo**,
**so that** eu entenda a tendência de imediato, sem precisar decifrar listas de termos c-TF-IDF (`iphone apple` → *"Prime Day pressiona preços de iPhone"*).

## Acceptance Criteria

1. **Estágio batch** em `src/insight/run.py` (espelhando `src/scores/run.py`): lê `doc_topics`, `topic_info`, `topic_terms` e `scores`; seleciona os **tópicos em ascensão** (`topic_id != -1`, `support_ok=True`, ordenados por `trend_score`) e monta, por tópico, o contexto com os **`top_artigos`** artigos mais representativos (título + trecho).
2. **Geração via LLM:** para cada tópico, chama `chat()` (Story 5.1) e obtém **(a)** um `label_llm` curto (≤ 8 palavras) e **(b)** um `why_summary` (2–4 frases). Prompts fixos em `src/insight/prompts.py` (rotulação + "por que sobe").
3. **Contrato A5:** grava `dados/insight/briefings.parquet` `{topic_id, label_llm, why_summary, model_name, generated_at}` **via `src/common/io.py`** (`salvar_parquet`).
4. **Reprodutibilidade:** usa `temperature=config.insight.temperature_batch` (0.0); registra `model_name` no A5 **e** no `run_manifest.json` (`atualizar_manifest(stage="insight", ...)`).
5. **Degradação graciosa:** se `chat()` devolve `None` para um tópico → registra fallback (mantém o `label` c-TF-IDF original e `why_summary` vazio) **com log** e segue; se o LLM estiver totalmente indisponível → o pipeline **termina sem quebrar** (A5 pode não ser gerado; nada estoura).
6. **Execução:** roda por `poetry run python -m src.insight.run` sobre os artefatos congelados; testes mockam `chat()` e validam o **schema A5** e o caminho de degradação. Suíte `pytest` **verde**.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled — validação por revisão manual (padrão do projeto).

## Tasks / Subtasks

- [x] **Task 1 — Montagem de contexto por tópico** (AC: 1)
  - [x] Ler artefatos (via `src/common/io.py`); filtrar tópicos em ascensão (`support_ok`, `!= -1`).
  - [x] Para cada tópico, juntar `doc_topics × corpus` e selecionar `top_artigos` (título + trecho).
- [x] **Task 2 — Prompts** (AC: 2)
  - [x] `src/insight/prompts.py`: template de rotulação e de "por que sobe" (fixos, versionáveis).
- [x] **Task 3 — Geração + montagem do A5** (AC: 2, 3, 4)
  - [x] Chamar `chat(temperature=config.insight.temperature_batch, max_tokens=config.insight.max_tokens)`.
  - [x] Montar DataFrame A5 com `model_name` (de `config.insight.model`) e `generated_at`.
  - [x] Persistir `dados/insight/briefings.parquet` via `salvar_parquet`.
- [x] **Task 4 — Manifesto + degradação** (AC: 4, 5)
  - [x] `atualizar_manifest(stage="insight", extras={"insight_model": ...})`.
  - [x] Fallback por tópico (`None` → mantém label c-TF-IDF, log); falha global → termina sem quebrar.
- [x] **Task 5 — Execução + testes** (AC: 6)
  - [x] `python -m src.insight.run` sobre o corpus congelado.
  - [x] `tests/test_insight.py`: mock de `chat()`, validação do schema A5, teste do fallback (`None`).
  - [x] `pytest` completo → verde.

## 📘 Guia de Desenvolvimento (para o integrante)

> Dificuldade 🟡 — **intermediária**. Você vai "costurar" dados que já existem, mandar para o `chat()` da 5.1 e salvar o resultado. O segredo está em **bons prompts** e em **não quebrar** quando o LLM falha.

### 🎯 O que esta story faz
Pega os tópicos que estão bombando (já ranqueados pela Fase 3) e, para cada um, pergunta ao LLM: *"olhando estes 5 artigos, dê um nome curto para o assunto e explique em 2-3 frases por que ele está crescendo."* O resultado vira uma tabelinha (`briefings.parquet`) que a Story 5.3 vai mostrar no dashboard. **Roda offline, uma vez** — não é ao vivo (isso é o RAG, story 5.5).

### 🧠 Pontos de rigor (não pular)
- **Espelhe o `scores/run.py`:** mesma cara de orquestrador (logs, ler → processar → `salvar_parquet` → `atualizar_manifest`). Não reinvente o estilo.
- **Contexto é tudo (grounding):** o LLM só deve falar do que está nos artigos. No prompt, deixe claro: *"baseie-se APENAS nos títulos/trechos abaixo"*. Isso reduz invenção.
- **`temperature=0`:** no batch queremos resultado estável e reprodutível — não criativo. Vem da config, não hardcode.
- **Degradação graciosa (AC5):** se o LLM cair no meio, você **não** perde tudo nem quebra o pipeline. Cada tópico tem seu fallback (o label velho c-TF-IDF).
- **Registre o modelo:** `model_name` no A5 e no manifesto — para a banca saber com qual modelo o briefing foi gerado.

### 🪜 Passo a passo (resumido — recommended-approach §5A)
1. Ler artefatos, filtrar tópicos em ascensão.
2. Por tópico: pegar `top_artigos` (título+trecho) → montar prompt.
3. `chat()` → `label_llm` + `why_summary`.
4. Montar `briefings.parquet` (+ `model_name`, `generated_at`) e salvar via `io.py`.
5. `atualizar_manifest`. Fallback quando `chat()` devolver `None`.

### ✅ Como saber que funcionou
- Existe `dados/insight/briefings.parquet` com uma linha por tópico em ascensão. *(AC1, AC3)*
- Os `label_llm` são nomes legíveis (não listas de termos); os `why_summary` batem com os artigos. *(AC2)*
- O manifesto tem o `insight_model`. *(AC4)*
- Se você desligar o Ollama e rodar, o comando **termina** (sem A5, ou com fallback), **sem traceback**. *(AC5)*
- `pytest` verde. *(AC6)*

### ⚠️ Erros comuns
- **Prompt que "puxa" invenção:** peça explicitamente para se ater aos artigos; peça saída curta.
- **Recriar artefatos da Fase 2/3:** esta story só **lê** deles; a única escrita é o A5.
- **Deixar exceção subir quando o LLM falha:** trate por tópico e globalmente.
- **`generated_at` com `datetime.now()` sem timezone:** siga o padrão de `io.py` (UTC ISO).

### 📚 Glossário (novos termos)
| Termo | Significado simples |
|---|---|
| Batch | Processa tudo de uma vez, offline (≠ ao vivo) |
| Briefing | O texto-resumo que o Analista IA gera |
| Grounding | Fazer o LLM responder só com base no material dado |
| Contrato A5 | O formato acordado do `briefings.parquet` |
| Fallback | Plano B quando algo falha (aqui: usar o label antigo) |

## Dev Notes

> Contexto de `docs/architecture/adr-002-camada-llm.md` (§Feature A, §Parâmetros, §Contrato A5) e `recommended-approach.md` (§3, §5A). NÃO inventar detalhes fora desses documentos.

- **Owner:** `src/insight/` (novo): `run.py` (orquestrador, espelha `src/scores/run.py`), `analista.py` (montagem de contexto + geração), `prompts.py` (templates). [Source: recommended-approach.md §3]
- **Entradas:** `dados/topics/{doc_topics,topic_info,topic_terms}.parquet`, `dados/scores/scores.parquet`, `dados/raw/corpus.parquet` (título/trecho). **Saída:** `dados/insight/briefings.parquet` (A5). [Source: adr-002 §Contrato A5]
- **Schema A5:** `{topic_id:int, label_llm:str, why_summary:str, model_name:str, generated_at:str(ISO)}`. [Source: adr-002 §Contrato A5]
- **Depende de:** `src/common/llm.chat()` (Story 5.1) e `config.insight.*` (`model`, `temperature_batch`, `max_tokens`, `top_artigos`). [Source: Story 5.1]
- **Persistência/manifesto:** exclusivamente via `src/common/io.py` (`salvar_parquet`, `atualizar_manifest`). Não criar IO novo. [Source: project-analysis.md §3.3]
- **Degradação graciosa:** mesmo princípio da Camada 2 (Story 3.3) — falha vira log + fallback, nunca exceção propagada. [Source: adr-002 §Consequências]

### Testing
- **Local (pytest):** `chat()` mockado (retorna label/why fixos); asserção do schema A5 e da ordenação por tópicos em ascensão. Teste do fallback: `chat()` → `None` ⇒ linha mantém label c-TF-IDF e não levanta erro. Sem chamada de rede real no CI. [Source: testing-strategy do projeto]

### Project Structure Notes
- `src/insight/` é a "Fase 5" e fica ao lado de `src/scores/` (Fase 3) — mesma hierarquia de estágios. `dados/insight/` acompanha `dados/scores/`. Sem conflito com a árvore atual. [Source: recommended-approach.md §3]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-08 | 0.1 | Draft inicial — Analista IA batch (contrato A5) | River (@sm) |
| 2026-07-08 | 0.2 | Validated GO (9/10) — Status: Draft → Ready. Should-fix: explicitar a coluna de origem do "trecho" do artigo (corpus A1 vs corpus_clean) na Task 1 | @po |
| 2026-07-08 | 0.3 | Status: Ready → InProgress — início da implementação (5.1 Done, chat() disponível) | Dex (@dev) |
| 2026-07-08 | 1.0 | Implementação completa (5 tasks, 6 ACs). 112 testes verdes; batch real 37/37 briefings (121,5s); E2E degradação com Ollama off (exit 0 em 0,5s). Status: InProgress → InReview | Dex (@dev) |
| 2026-07-08 | 1.1 | Quality gate PASS (8/8 checks) — conformidade ADR-002 §A, consistência A5×scores verificada. Status: InReview → Done | Aria (@architect) |

## Dev Agent Record
### Agent Model Used
claude-fable-5 (Claude Code)
### Debug Log References
- `poetry run pytest` → **112 passed** (100 anteriores + 12 novos de `tests/test_insight.py`).
- Execução real: `poetry run python -m src.insight.run` → **37/37 briefings via LLM, 0 fallbacks, 121,5s** (qwen2.5:14b na GPU). A5 gravado com 37 linhas; manifesto com `insight_model` e `stage_versions.insight="5.2"`.
- Prova E2E do AC5: `systemctl stop ollama` → run termina em **0,5s, exit 0, sem traceback**, A5 anterior preservado; curto-circuito na 1ª falha total evitou 74 timeouts.
### Completion Notes List
- **AC1:** `selecionar_topicos_ascensao` filtra `topic_id != -1` + `support_ok`, ordena por `trend_score` desc; `montar_contexto` seleciona `top_artigos` por `probabilidade` desc do `doc_topics` × corpus A1.
- **Should-fix @po resolvido:** o "trecho" vem da coluna `texto` do **corpus A1** (`dados/raw/corpus.parquet`) — texto original legível, não o `corpus_clean` normalizado (que removeria pontuação/maiúsculas e prejudicaria o LLM). Documentado no docstring de `analista.py`.
- **AC2:** dois prompts fixos em `prompts.py` (rotulação ≤8 palavras + "por que sobe" 2–4 frases), ambos grounded ("baseie-se APENAS nos artigos"); `limpar_label` normaliza e trunca em 8 palavras (bug de ordem strip/splitlines capturado por teste e corrigido).
- **AC3/AC4:** A5 `{topic_id, label_llm, why_summary, model_name, generated_at}` via `salvar_parquet`; `temperature_batch=0.0` da config; `generated_at` UTC ISO (padrão `io.py`); manifesto atualizado com params completos.
- **AC5:** fallback por tópico (label c-TF-IDF + why vazio + log) e curto-circuito global: se o 1º tópico falha nas 2 chamadas sem nenhum sucesso anterior, assume LLM fora e não martela os demais. Falha global → A5 não gravado, exit 0.
- **Config estendida:** `trecho_max_chars=500` adicionado a `InsightParams` + `config.yaml` (truncamento do trecho não podia ser número mágico — Coding Standards).
- **Decisão de design:** 2 chamadas por tópico (rotulação e why separados) em vez de 1 chamada com JSON — parsing robusto com modelo local 14B, sem risco de JSON malformado.
- **Opcional não incluído:** encaixe no `run_all.py` — o estágio roda standalone (`python -m src.insight.run`); integrar ao orquestrador global pode entrar com a 5.3 se o time quiser.
### File List
- `src/insight/__init__.py` **[novo]**
- `src/insight/run.py` **[novo]** — orquestrador (espelha `scores/run.py`)
- `src/insight/analista.py` **[novo]** — seleção, contexto, geração, fallback
- `src/insight/prompts.py` **[novo]** — templates grounded (rotulação + why)
- `src/common/config.py` **[modificado]** — +`trecho_max_chars` em `InsightParams`
- `config/config.yaml` **[modificado]** — +`trecho_max_chars`
- `tests/test_insight.py` **[novo]** — 12 testes (seleção, contexto, label, briefing, A5, degradação)
- `dados/insight/briefings.parquet` **[novo]** — contrato A5 gerado (37 tópicos)
- `dados/run_manifest.json` **[modificado]** — stage insight

## QA Results

### Quality Gate — @architect (Aria), 2026-07-08

**Verdict: PASS** ✅

| Check | Resultado |
|---|---|
| Conformidade ADR-002 §Feature A (precompute-then-serve) | ✅ espelha `scores/run.py`; estágio aditivo e standalone |
| Contrato A5 (AC3) | ✅ schema exato; consistência A5×scores perfeita (37 = support_ok ∧ ≠-1); 0 duplicatas |
| Grounding e qualidade (AC2) | ✅ prompts restritivos; 0 labels >8 palavras; 0 why vazios no run real; amostras fiéis aos artigos |
| Degradação graciosa (AC5) | ✅ 2 níveis (tópico + curto-circuito); E2E real: Ollama off → exit 0 em 0,5s, A5 preservado |
| Reprodutibilidade (AC4) | ✅ temperature_batch=0.0; model_name no A5 + manifesto; A5 versionado (mitigação do ADR p/ demo) |
| Suíte e lint (AC6) | ✅ 112 passed; ruff limpo; batch real 37/37 em 121,5s |
| Should-fix @po | ✅ trecho = corpus A1 `texto` (justificado: legibilidade > normalização) |
| Config sem número mágico | ✅ `trecho_max_chars` em `InsightParams` + YAML |

**Observações não-bloqueantes:**
- Log diz "fallback c-TF-IDF" quando só o "why" falha, mas a linha mantém corretamente o label gerado — imprecisão cosmética de log.
- Curto-circuito global pode rebaixar o batch inteiro se a 1ª falha for transitória — trade-off correto (evita 74 timeouts); re-execução resolve.

---

> **Nota de processo (@sm):** ClickUp/code-intel não configurados → skip gracioso.
