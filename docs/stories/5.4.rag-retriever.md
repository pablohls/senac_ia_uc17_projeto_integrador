# Story 5.4: RAG — recuperador semântico sobre os embeddings [SHOULD]

## Status

Ready

## Executor Assignment

```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [git, ruff, pytest]
```

> **Nota da equipe:** Dificuldade 🟡 (**intermediária** — busca vetorial, sem treino). Prioridade **SHOULD** (diferencial "converse com as tendências"; protegido pelo plano garantido do Analista IA MUST). **Reaproveita `embeddings.npy` já pronto.** Referência: `docs/architecture/adr-002-camada-llm.md` (§Feature B), `docs/architecture/recommended-approach.md` (§3, §5B).

## Story

**As a** motor de resposta do chatbot RAG,
**I want** recuperar os trechos de artigos mais semelhantes a uma pergunta em PT-BR,
**so that** a resposta ao usuário seja **fundamentada em artigos reais e citáveis** — não em conhecimento genérico do modelo.

## Acceptance Criteria

1. `src/rag/retriever.py` **reaproveita `dados/processed/embeddings.npy` + `embeddings_index.parquet`** (NÃO recomputar embeddings do corpus).
2. Embeda a **pergunta** com o **mesmo modelo Sentence-Transformers** do pipeline (`config.embedding_model`) e retorna os **`top_k`** (`config.insight.rag_top_k`) artigos por **similaridade de cosseno**.
3. Cada resultado traz os **metadados para citação**: `doc_id`, `titulo`, `url`, `fonte` (join com `corpus`), além do `score` de similaridade.
4. **Degradação graciosa:** ausência de `embeddings.npy`/índice → retorna lista vazia com log (o chamador, Story 5.5, lida com isso). Nunca levanta exceção não tratada.
5. Testes: com uma matriz de embeddings pequena/sintética, asserção de que os `top_k` retornados são os de maior cosseno e vêm com metadados. `pytest` **verde**.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled — validação por revisão manual (padrão do projeto).

## Tasks / Subtasks

- [ ] **Task 1 — Carregar índice de embeddings** (AC: 1, 4)
  - [ ] Ler `embeddings.npy` + `embeddings_index.parquet`; validar alinhamento; log + vazio se faltarem.
- [ ] **Task 2 — Embed da pergunta + cosseno** (AC: 2)
  - [ ] Carregar o modelo ST (mesmo `config.embedding_model`); embeddar a query; cosseno vs matriz; `top_k`.
- [ ] **Task 3 — Metadados p/ citação** (AC: 3)
  - [ ] Join `doc_id → corpus` (`titulo, url, fonte`); devolver estrutura `{doc_id, titulo, url, fonte, score}`.
- [ ] **Task 4 — Testes** (AC: 5)
  - [ ] `tests/test_retriever.py` com matriz sintética; asserção de ordenação por cosseno + presença de metadados.

## 📘 Guia de Desenvolvimento (para o integrante)

> Dificuldade 🟡 — **intermediária**. A boa notícia: o trabalho pesado (transformar 6.423 artigos em vetores) **já foi feito** na Fase 2. Aqui você só compara a pergunta com esses vetores.

### 🎯 O que esta story faz
Quando o usuário perguntar algo, esta peça encontra **quais artigos falam sobre aquilo**. Ela transforma a pergunta em um vetor (com o mesmo modelo que já usamos) e mede quais artigos têm o vetor mais "próximo" (cosseno). Devolve os `top_k` com título e link — para a Story 5.5 responder **citando as fontes**.

### 🧠 Pontos de rigor (não pular)
- **Reúso obrigatório (AC1):** use `embeddings.npy` que já existe. **Não** re-embede o corpus — seria caro e desnecessário (princípio IDS: REUSE).
- **Mesmo modelo dos dois lados:** a pergunta tem que ser embeddada com o **mesmo** `embedding_model` do corpus, senão a comparação não faz sentido.
- **Cosseno, não distância euclidiana:** normalizar e usar produto interno / cosseno.
- **Degradação graciosa (AC4):** sem os arquivos de embedding, devolva lista vazia (a 5.5 mostra "sem base para responder").

### 🪜 Passo a passo (resumido — recommended-approach §5B)
1. Carregar `embeddings.npy` + índice.
2. Embeddar a pergunta com o modelo ST.
3. Cosseno pergunta × matriz → pegar `top_k` índices.
4. Mapear índice → `doc_id` → metadados do `corpus`.

### ✅ Como saber que funcionou
- `retrieve("o que houve com iPhone?", top_k=6)` devolve 6 artigos plausíveis, com título/URL. *(AC2, AC3)*
- Perguntas diferentes trazem artigos diferentes e coerentes. *(AC2)*
- Sem `embeddings.npy`, devolve `[]` com log — sem erro. *(AC4)*
- `pytest` verde. *(AC5)*

### ⚠️ Erros comuns
- **Recarregar o modelo ST a cada pergunta:** carregue uma vez (cache) — é o passo lento.
- **Desalinhar índice e matriz:** a linha `i` de `embeddings.npy` tem que corresponder à linha `i` de `embeddings_index.parquet`.
- **Esquecer de normalizar** antes do cosseno.

### 📚 Glossário
| Termo | Significado simples |
|---|---|
| RAG | "Buscar e então responder" — responde usando trechos recuperados |
| Embedding | Vetor que representa o significado de um texto |
| Cosseno | Medida de similaridade entre dois vetores (1 = idênticos) |
| top_k | Quantos trechos mais parecidos trazer |
| Retriever | A peça que faz a busca |

## Dev Notes

> Contexto de `adr-002-camada-llm.md` (§Feature B) e `recommended-approach.md` (§5B). NÃO inventar.

- **Owner:** `src/rag/retriever.py` (novo). Função: `retrieve(pergunta: str, top_k: int) -> list[dict]`. [Source: recommended-approach.md §5B]
- **Reúso (IDS):** `dados/processed/embeddings.npy` (19MB) + `embeddings_index.parquet` já persistidos pela Fase 2 — não recomputar. [Source: project-analysis.md §4]
- **Modelo de embedding:** o **mesmo** `config.embedding_model` (`paraphrase-multilingual-mpnet-base-v2`). [Source: config/config.yaml]
- **Metadados de citação:** join `doc_id → dados/raw/corpus.parquet` (`titulo, url, fonte`). [Source: src/dashboard/app.py — mesmo join do drill-down]
- **Param:** `config.insight.rag_top_k` (Story 5.1). [Source: adr-002 §Parâmetros]

### Testing
- **Local (pytest):** matriz sintética pequena de embeddings + índice; asserção de que os `top_k` são os de maior cosseno e trazem metadados. Sem download de modelo no CI (mockar/dublar o encoder da pergunta). [Source: testing-strategy do projeto]

### Project Structure Notes
- `src/rag/` é novo, ao lado de `src/insight/` e `src/scores/`. `retriever.py` é a metade "busca" do RAG; a metade "responde" é a Story 5.5. [Source: recommended-approach.md §3]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-08 | 0.1 | Draft inicial — retriever RAG (reúso de embeddings) | River (@sm) |
| 2026-07-08 | 0.2 | Validated GO (9/10) — Status: Draft → Ready. Reúso de embeddings.npy bem especificado (IDS); confirmar cache do encoder da pergunta na implementação | @po |

## Dev Agent Record
### Agent Model Used
_(a preencher pelo @dev)_
### Debug Log References
_(a preencher)_
### Completion Notes List
_(a preencher)_
### File List
_(a preencher — previsto: `src/rag/__init__.py`, `src/rag/retriever.py` [novos], `tests/test_retriever.py` [novo])_

## QA Results
_(a preencher)_

---

> **Nota de processo (@sm):** ClickUp/code-intel não configurados → skip gracioso.
