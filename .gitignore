

# Environment & Secrets (AIOX)
.env
.env.local
.env.*.local
*.key
*.pem

# Dependencies (AIOX)
node_modules/
node_modules

# Build & Logs (AIOX)
dist/
build/
*.log
logs/

# IDE & OS (AIOX)
.DS_Store
Thumbs.db
.idea/
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt

# ---- Projeto (entregável SENAC UC17) ----

# Tooling de IA / framework (não fazem parte do entregável)
.aiox-core/
.aiox/
.claude/
.antigravity/
.codex/
.cursor/
.gemini/
.kimi/
.github/

# Documentos internos de processo/sessão (não fazem parte do entregável;
# mantidos localmente e distribuídos à parte entre os desenvolvedores)
docs/handoff-*.md
docs/runlogs/



# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# Modelos / embeddings / artefatos pesados
*.pt
*.bin
*.cache
embeddings/
models/

# ---- Política de dados (contratos A1–A4) ----
# VERSIONADOS: dados/raw/ (corpus A1 congelado + urls), dados/topics/,
#   dados/scores/, dados/run_manifest.json e embeddings_index.parquet —
#   leves; o dashboard funciona logo após o clone, sem rodar o pipeline.
# IGNORADOS (pesados e 100% regeneráveis a partir do corpus A1 via
#   `poetry run sonar`):
dados/processed/corpus_clean.parquet
dados/processed/embeddings.npy
